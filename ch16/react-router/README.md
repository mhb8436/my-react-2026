# install dependecies
```bash
npm i react-router-dom 
npm run dev

```
- react-router-dom 라이브러리는 외부 라이브러잉넫 
- React 에는 자체 라우팅 기능이 없어서 이 라이브러리를 표준 처럼 사용하고 있어요

# BrowserRouter 
- <App/> 감싼이유는 
- <App/> 하위의 모든 컴포넌트에서 Link, Routes, useParams, useNavigate 도구를 마음것 사용하기 위해서는 BrowserRouter 
